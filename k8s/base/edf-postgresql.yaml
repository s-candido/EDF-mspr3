apiVersion: apps/v1
kind: Deployment
metadata:
  name: edf-postgresql
  namespace: ml-platform
  labels:
    app: edf-postgresql
    component: database
spec:
  replicas: 1
  selector:
    matchLabels:
      app: edf-postgresql
  template:
    metadata:
      labels:
        app: edf-postgresql
    spec:
      containers:
        - name: postgresql
          image: postgres:13
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: ml-platform-secrets
                  key: POSTGRES_USER_EDF
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ml-platform-secrets
                  key: POSTGRES_PASSWORD_EDF
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: ml-platform-secrets
                  key: POSTGRES_DB_EDF
          volumeMounts:
            - name: edf-data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: edf-data
          persistentVolumeClaim:
            claimName: postgres-edf-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: edf-postgresql
  namespace: ml-platform
spec:
  selector:
    app: edf-postgresql
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  clusterIP: None
